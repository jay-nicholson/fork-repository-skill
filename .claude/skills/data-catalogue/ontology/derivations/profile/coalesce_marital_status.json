{
  "id": "coalesce_marital_status",
  "name": "Coalesce Marital Status",
  "description": "Get marital status from loan_application.applicant, falling back to member if not available. Used in Header component.",
  "steps": [
    {
      "id": "get_loan_app",
      "operation": "identity",
      "inputs": [
        {
          "derivation": "select_latest_loan_application"
        }
      ],
      "description": "Get latest loan application for user"
    },
    {
      "id": "get_member",
      "operation": "identity",
      "inputs": [
        {
          "derivation": "filter_user_member"
        }
      ],
      "description": "Get member for user"
    },
    {
      "id": "extract_from_loan_app",
      "operation": "select",
      "inputs": [
        {
          "step": "get_loan_app"
        },
        {
          "literal": "applicant.marital_status"
        }
      ],
      "description": "Extract marital_status from loan_application.applicant"
    },
    {
      "id": "extract_from_member",
      "operation": "select",
      "inputs": [
        {
          "step": "get_member"
        },
        {
          "literal": "relationship_status"
        }
      ],
      "description": "Extract relationship_status from member"
    },
    {
      "id": "coalesce_status",
      "operation": "coalesce",
      "inputs": [
        {
          "step": "extract_from_loan_app"
        },
        {
          "step": "extract_from_member"
        }
      ],
      "description": "Use loan_application.applicant.marital_status if available, otherwise member.relationship_status"
    }
  ],
  "output": {
    "step": "coalesce_status",
    "description": "Marital status (loan_application ?? member)",
    "type": "text"
  },
  "depends_on": {
    "derivations": [
      "select_latest_loan_application",
      "filter_user_member"
    ],
    "fields": [
      "loan_application.applicant.marital_status",
      "member.relationship_status"
    ]
  },
  "source": {
    "repo": "brightmatch",
    "path": [
      "src",
      "shared",
      "queries",
      "member",
      "map_profile.go"
    ],
    "lines": [
      77,
      81
    ],
    "symbol": "MapUserProfile"
  },
  "ui_outputs": [
    {
      "component": "Header",
      "label": "Marital Status",
      "format": "text"
    }
  ]
}
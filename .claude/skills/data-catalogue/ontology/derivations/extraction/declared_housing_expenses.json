{
  "id": "declared_housing_expenses",
  "name": "Declared Housing Expenses",
  "description": "Extract and normalize housing expenses from loan application to monthly amount",
  "steps": [
    {
      "id": "raw_amount",
      "operation": "select",
      "inputs": [
        {
          "derivation": "select_latest_loan_application"
        },
        {
          "literal": "expenses.housing_amount"
        }
      ]
    },
    {
      "id": "frequency",
      "operation": "select",
      "inputs": [
        {
          "derivation": "select_latest_loan_application"
        },
        {
          "literal": "expenses.housing_frequency"
        }
      ]
    },
    {
      "id": "monthly",
      "operation": "normalise_monthly",
      "inputs": [
        {
          "step": "raw_amount"
        },
        {
          "step": "frequency"
        }
      ]
    }
  ],
  "output": {
    "step": "monthly",
    "unit": "AUD_monthly"
  },
  "depends_on": {
    "derivations": [
      "select_latest_loan_application"
    ]
  },
  "source": {
    "repo": "brightmatch",
    "path": [
      "src",
      "shared",
      "queries",
      "member",
      "map_declared.go"
    ],
    "lines": [
      30,
      32
    ],
    "symbol": "MapDeclared - MonthlyHousingExpenses"
  }
}

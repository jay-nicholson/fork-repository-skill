{
  "id": "extract_credit_account_name",
  "name": "Extract Credit Account Name",
  "description": "Extract account name from CCR credit account for display",
  "steps": [
    {
      "id": "get_accounts",
      "operation": "identity",
      "inputs": [{ "derivation": "filter_user_credit_accounts" }],
      "description": "Get user's CCR credit accounts"
    },
    {
      "id": "extract_field",
      "operation": "select",
      "inputs": [
        { "step": "get_accounts" },
        { "literal": "name" }
      ],
      "description": "Extract name field from each account"
    }
  ],
  "output": {
    "step": "extract_field",
    "description": "Credit provider name"
  },
  "depends_on": {
    "derivations": ["filter_user_credit_accounts"],
    "fields": ["credit_account.name"]
  },
  "ui_outputs": [
    {
      "component": "AccountInfo",
      "label": "Account Name",
      "format": "text"
    }
  ],
  "source": {
    "repo": "brightmatch-admin-portal",
    "path": ["src", "views", "summary-v2", "components", "credit-accounts", "CreditAccountCard.tsx"],
    "lines": [38, 38],
    "symbol": "name"
  }
}
